<%= form_with(model: profession, local: true) do |form| %>
  <% if profession.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(profession.errors.count, "error") %> prohibited this profession from being saved:</h2>

      <ul>
      <% profession.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 <div class="field">
    <%= form.label :domain_id %>
    <%= form.collection_select :domain_id,   @domains,  :id, :title , {prompt: "select"}, class: "form-control" %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :profession_title, class: "form-control" %>
  </div>
 

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
