<%= form_with(model: user_language, local: true) do |form| %>
  <% if user_language.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_language.errors.count, "error") %> prohibited this user_language from being saved:</h2>

      <ul>
      <% user_language.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :user_language_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
