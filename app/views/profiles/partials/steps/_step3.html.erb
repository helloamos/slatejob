<div class="box box-primary">
	<div class="box-body">
		<div class="row">
			<div class="col-md-12">
			
        		<div class="form-group">
        			<%= forms.label t(:profession) %>
        			<%= forms.collection_select :profession_id,  @domains, :id, :title, {prompt: "Sélectioner une profession"}, class: "form-control",  required: true %>
        		</div>
    		
    	
    	
			<div class="form-group">
				<%= fields_for(@project_skill) do |ps| %>
					<%= ps.label "Quelles compétences sont requises?" %>
					<%= collection_select(:skills, :id,  @all_skills, :id, :title, {}, {class: "form-control chosen-select", multiple: true}) %>
				<% end %>
			</div><!-- end nested -->

			
    		</div>
    	</div>

	</div>
</div>
<ul class="list-inline pull-right">
	<li><button type="button" class="btn btn-primary">Continue</button></li>
 </ul>