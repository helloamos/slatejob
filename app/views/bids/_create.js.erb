$("ul.errors").html("");
<% if @bid.errors.any? %>
  <% @bid.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"));
  <% end %>
<% else %>
  $(".bids-list").html("<%= escape_javascript(render 'bids') %>");
  $("#bid-modal").modal("hide");
 
<% end %>