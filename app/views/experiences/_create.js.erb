$("ul.errors").html("");
<% if @experience.errors.any? %>
  <% @experience.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"));
  <% end %>
<% else %>
  $(".experiences-index").html("<%= escape_javascript(render 'index') %>");
  $("#experiences-modal").modal("hide");
<% end %>